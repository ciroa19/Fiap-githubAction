name: Test workflow

on:
  push:
    branches: 
      - main

jobs:
  test_job:
    name: Executando o Job Teste
    runs-on: ubuntu-latest  # Corrigido para 'ubuntu-latest'
    steps:
      - name: Step 1
        uses: actions/checkout@v3
      - name: Docker Login 
        uses: docker/login-action@v3
        with:      
          username: 'Ciroa'
          password: ${{ secrets.TESTE_GITHUB }}
